# Task Plan: Database-backed notifications

> **Plan ID:** `2026-09-03-database-backed-notifications`  
> **Date:** 2026-09-03  
> **Author / Agent:** Codex  
> **Status:** Completed  
> **Branch / PR:** `main`

## 1. Problem Statement & Business Objective

Persist notification events and per-recipient read receipts in Supabase so the same account sees consistent unread counts and evidence across browsers. Keep a local cache/fallback because the current prototype uses demo session roles rather than Supabase Auth.

## 2. Affected Routes & Modules

- `sql/create_notification_store.sql`: normalized event/recipient schema, constraints, grants, RLS and indexes.
- `assets/ecmis-app.js`: database hydration, optimistic writes, receipt persistence and offline fallback.
- `notifications.html`: load Supabase client and refresh the center from the database.
- `/res/` mirrors generated by `npm run sync`.
- Notification E2E and documentation.

## 3. Anti-Regression Pre-Check

- [x] Preserve the required case-type table column.
- [x] Do not expose the agenda registry to chairman/affairs.
- [x] Use only the shared `ECMIS.getSupabaseClient()` singleton.
- [x] Run `npm run sync` after root HTML changes.
- [x] Do not alter government A4 geometry.
- [x] Do not bypass commit hooks.

## 4. Implementation Tasks

- [x] Add normalized database schema and idempotency constraints.
- [x] Add database-backed hydration/write-through behavior to `NotificationStore`.
- [x] Persist first-read timestamps per recipient without overwriting them.
- [x] Keep local cache fallback for offline/static prototype use.
- [x] Sync routes and run CI, integration and notification E2E.

## 5. Verification

- [x] Database migration exposes normalized constraints, indexes, grants and prototype RLS policies.
- [x] Existing synchronous UI renders from cache then refreshes from Supabase.
- [x] Event creation remains idempotent and receipts remain account-isolated.
- [x] Root and `/res/` routes pass CI and Playwright.

## 6. Completion & Sign-off

- **Completed Date:** 2026-09-03
- **Commit Reference:** Pending
- **Notes:** Migration and demo seed applied to Supabase. REST verification returned one demo event and three unread recipient rows; manual browser verification showed the database source and unread item for Somchai.I. Database policies are prototype-compatible with demo roles; production deployment must replace demo role identifiers with authenticated user UUID checks.
