# คู่มือและมาตรฐานการพัฒนา E-CMIS กิจกรรมที่ ๗ (CONTRIBUTING.md)

ยินดีต้อนรับสู่โปรเจกต์ **E-CMIS Activity 7 (การประชุมและรายงานวินิจฉัยชี้มูล คณะกรรมการ ป.ป.ท.)**  
เอกสารฉบับนี้กำหนดมาตรฐานการทำงานร่วมกันในทีม เพื่อรักษาความปลอดภัย ความถูกต้องของเอกสารราชการ และป้องกันความผิดพลาดระดับ Production

---

## 🚀 1. สมาชิกใหม่ในทีมเริ่มทำงาน (Onboarding)

เมื่อ Clone โปรเจกต์ไปแล้ว ให้เปิด Terminal ที่โฟลเดอร์โปรเจกต์ แล้วรันคำสั่งเดียว:

```bash
npm run setup
```

> 💡 **หน้าที่ของคำสั่งนี้:** จะติดตั้ง Git Pre-commit Hook (`.git/hooks/pre-commit`) ลงในเครื่องให้อัตโนมัติ โดยที่ทุกคนไม่ต้องตั้งค่าใดๆ เพิ่มเติม

---

## 🛡️ 2. ระบบ Auto Quality Gate (CI & Anti-Regression Guard)

ทุกครั้งที่มีการสั่ง `git commit` ระบบจะรัน `scripts/ci-check.js` ตรวจสอบความถูกต้อง 5 ด่านทันที:

1. **JavaScript Syntax Check:** ตรวจสอบไวยากรณ์ JS ทุกไฟล์ว่าไม่มี Syntax Error หรือ Conflict Markers
2. **Dual-Route Mirroring (Root ↔ `/res/`):** ตรวจสอบว่าไฟล์ HTML ฝั่ง Root มีคู่ตรงกับ `/res/` ครบถ้วน 100%
3. **Zero-404 Internal Link Audit:** ตรวจสอบทุกลิงก์ภายในว่าเปิดได้จริง ไม่พบ 404
4. **Anti-Regression Governance:** ป้องกันการเผลอลบคอลัมน์สำคัญ (เช่น *ประเภทเรื่อง*) หรือกฎธุรกิจหลัก
5. **A4 Document & Print Layout Integrity:** ควบคุมระยะขอบและสัดส่วน A4 ให้ตรงตามระเบียบสารบรรณ

> ❌ **หากไม่ผ่าน:** Git จะปฏิเสธการ Commit พร้อมระบุไฟล์และบรรทัดที่ต้องแก้ไขทันที  
> ✅ **หากผ่าน 100%:** จึงจะอนุญาตให้ Commit ได้ ทำให้โค้ดของทุกคนมีมาตรฐานความปลอดภัยสูงสุดเสมอ

---

## 📋 3. สรุป 3 คำสั่งที่ทุกคนในทีมต้องใช้

| คำสั่ง | เมื่อไหร่ที่ต้องใช้ | หน้าที่ของคำสั่ง |
| :--- | :--- | :--- |
| `npm run setup` | ครั้งแรกหลัง Clone โปรเจกต์ | ติดตั้ง Git Hook ลงเครื่องอัตโนมัติ |
| `npm run sync` | หลังแก้ไขไฟล์ HTML ฝั่ง Root | ซิงค์ไฟล์ไป `/res/` พร้อมแปลง Asset Path ให้อัตโนมัติ |
| `npm test` | ก่อน Commit งาน | ตรวจสอบ Syntax, Zero-404 Links และ Anti-Regression ทั้งระบบ |

---

## 📌 กฎเหล็กประจำโปรเจกต์ (Anti-Regression Rules)
1. **ห้ามเปลี่ยนรูปแบบเอกสารราชการตั้งต้น** เว้นแต่จะมีมติหรือข้อกำหนดชัดเจน
2. **ห้ามลืมซิงค์ไฟล์ `/res/`** ให้ใช้ `npm run sync` ทุกครั้งหลังแก้ไข
3. **ห้าม Commit หาก `npm test` ไม่ผ่าน 100%**
